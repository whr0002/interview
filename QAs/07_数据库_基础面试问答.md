---
title: "Database Interview Q&A"
category: "database"
tags: ["database", "sql", "transactions", "indexes", "interview"]
language: "en"
source_type: "interview-notes"
---

# Database Interview Q&A

## Knowledge Notes

This guide is split into common and advanced database interview questions with concise, interview-ready answers.

## Common Database Interview Questions

### 1. What is a database?
A database is an organized system for storing, managing, and retrieving data efficiently. It allows applications and users to insert, update, query, and analyze information in a structured way.

### 2. What is the difference between SQL and NoSQL databases?
SQL databases are relational, use structured schemas, and are typically queried with SQL. They are strong when you need joins, transactions, and strict consistency. NoSQL databases are often schema-flexible and are designed for use cases such as large-scale document storage, caching, graph traversal, or high-throughput distributed workloads.

### 3. What is a primary key?
A primary key is a column or set of columns that uniquely identifies each row in a table. It must contain unique values and cannot be null.

### 4. What is a foreign key?
A foreign key is a column or set of columns in one table that references the primary key of another table. It enforces referential integrity between related tables.

### 5. What is normalization?
Normalization is the process of organizing data to reduce redundancy and improve data integrity. Common normal forms include 1NF, 2NF, and 3NF.

### 6. When would you denormalize a database?
You denormalize when read performance is more important than strict normalization, especially in reporting, analytics, or high-read systems where reducing joins improves speed.

### 7. What is the difference between DELETE, TRUNCATE, and DROP?
- `DELETE` removes selected rows and can be used with a `WHERE` clause.
- `TRUNCATE` removes all rows from a table quickly, usually with minimal logging.
- `DROP` removes the entire table structure and its data.

### 8. What is the difference between WHERE and HAVING?
`WHERE` filters rows before grouping. `HAVING` filters groups after `GROUP BY` has been applied.

### 9. What is a JOIN?
A JOIN combines rows from two or more tables based on a related column.

### 10. What is the difference between INNER JOIN and LEFT JOIN?
`INNER JOIN` returns only matching rows from both tables. `LEFT JOIN` returns all rows from the left table and matching rows from the right table, with nulls when no match exists.

### 11. What is an index?
An index is a data structure that improves lookup speed on one or more columns. It helps queries avoid full table scans, though it adds storage cost and can slow down inserts, updates, and deletes.

### 12. What are constraints in a database?
Constraints are rules enforced on data to maintain integrity. Common examples are `PRIMARY KEY`, `FOREIGN KEY`, `UNIQUE`, `NOT NULL`, and `CHECK`.

### 13. What is the difference between UNIQUE and PRIMARY KEY?
Both enforce uniqueness, but a primary key uniquely identifies the main record in a table and cannot be null. A table has one primary key, while it may have multiple unique constraints depending on the database engine.

### 14. What is a transaction?
A transaction is a sequence of database operations treated as a single unit of work. It either fully succeeds and commits, or fails and rolls back.

### 15. What does ACID mean?
- **Atomicity**: all operations in a transaction succeed or none do.
- **Consistency**: the database remains in a valid state.
- **Isolation**: concurrent transactions do not interfere improperly.
- **Durability**: committed changes survive failures.

### 16. What is the difference between a clustered and non-clustered index?
A clustered index determines the physical order of data storage in the table. A non-clustered index stores a separate structure with pointers to the actual rows. A table generally has one clustered index but can have many non-clustered indexes.

### 17. What is a composite index?
A composite index is an index on multiple columns. It is useful when queries commonly filter or sort by those columns together.

### 18. What is the N+1 query problem?
The N+1 query problem happens when an application performs one query to fetch a list of records and then executes an additional query for each record to fetch related data. It causes unnecessary round trips and poor performance.

### 19. What is a view?
A view is a virtual table based on the result of a query. It can simplify complex queries, improve reusability, and sometimes provide a security abstraction.

### 20. What is a stored procedure?
A stored procedure is a reusable set of SQL statements stored in the database. It can encapsulate business logic, improve consistency, and reduce repeated SQL in applications.

## Advanced Database Interview Questions

### 21. What are database isolation levels?
Isolation levels define how transactions interact with one another:
- `Read Uncommitted`: allows dirty reads.
- `Read Committed`: prevents dirty reads.
- `Repeatable Read`: prevents dirty and non-repeatable reads.
- `Serializable`: highest isolation, prevents dirty, non-repeatable, and phantom reads, but reduces concurrency.

### 22. What are dirty reads, non-repeatable reads, and phantom reads?
- **Dirty read**: reading uncommitted data from another transaction.
- **Non-repeatable read**: getting different values when reading the same row twice in one transaction.
- **Phantom read**: seeing different sets of rows when rerunning a query in one transaction because another transaction inserted or deleted matching rows.

### 23. What is a deadlock?
A deadlock happens when two or more transactions wait on each other to release locks, so none can proceed. Databases usually detect deadlocks and abort one transaction.

### 24. How can you reduce the chance of deadlocks?
You can reduce deadlocks by accessing tables in a consistent order, keeping transactions short, indexing properly to reduce lock duration, and avoiding unnecessary user interaction inside transactions.

### 25. What is optimistic locking vs pessimistic locking?
Optimistic locking assumes conflicts are rare and checks for changes before commit, often using a version column. Pessimistic locking locks data early to prevent concurrent modification, which is safer for contention-heavy scenarios but reduces concurrency.

### 26. How would you optimize a slow SQL query?
Start by checking the execution plan. Look for full table scans, expensive joins, sorts, and missing indexes. Then reduce scanned rows, add or refine indexes, rewrite inefficient predicates, avoid unnecessary columns, and verify whether schema or query structure should change.

### 27. What is an execution plan?
An execution plan shows how the database engine will execute a query, including scans, seeks, joins, sorts, and estimated or actual costs. It is one of the main tools for diagnosing performance issues.

### 28. Why might a query not use an index?
Common reasons include low selectivity, outdated statistics, small table size, functions applied to indexed columns, implicit type conversions, leading wildcards in `LIKE`, or because the optimizer estimates a scan is cheaper.

### 29. What is partitioning?
Partitioning splits a large table or index into smaller logical pieces while still presenting it as one object. It can improve manageability and, in some cases, query performance when filters align with the partition key.

### 30. What is sharding?
Sharding distributes data across multiple independent database servers, usually by a shard key. It helps scale horizontally, but it increases complexity around joins, transactions, and rebalancing.

### 31. What is replication?
Replication is the process of copying data from one database server to others. It is commonly used for high availability, disaster recovery, and read scaling.

### 32. What is the CAP theorem?
The CAP theorem says that in a distributed system, during a network partition you can choose at most two of the following three guarantees: consistency, availability, and partition tolerance. Since partitions can happen, systems often make tradeoffs between consistency and availability.

### 33. What is eventual consistency?
Eventual consistency means that if no new updates occur, all replicas will eventually converge to the same value. It is common in distributed systems that prioritize availability and performance.

### 34. What is a covering index?
A covering index includes all the columns needed to satisfy a query, so the database can return results directly from the index without going back to the base table.

### 35. What is cardinality in databases?
Cardinality can refer to relationship type, such as one-to-many, or to the number of distinct values in a column. In query optimization, higher cardinality often makes an index more useful.

### 36. What is the difference between OLTP and OLAP?
OLTP systems handle many short, transactional operations such as inserts and updates in business applications. OLAP systems are optimized for analytical queries, aggregations, and reporting across large datasets.

### 37. What are materialized views?
Materialized views store the results of a query physically, unlike standard views which are computed at query time. They improve read performance for expensive queries but require refresh management.

### 38. What is database caching?
Database caching stores frequently accessed data in a faster layer, such as Redis or application memory, to reduce database load and improve latency.

### 39. What are database statistics, and why do they matter?
Statistics describe data distribution and help the query optimizer estimate row counts and choose efficient execution plans. Poor or stale statistics can cause bad plans and slow queries.

### 40. What is a schema migration?
A schema migration is a controlled change to the database structure, such as adding tables, columns, indexes, or constraints. Good migration practice includes versioning, rollback planning, and backward compatibility for live systems.

## Scenario-Based Questions and Answers

### 41. How would you design a database for an e-commerce application?
I would start with core entities such as users, products, categories, carts, orders, order items, payments, and inventory. I would normalize transactional tables, add indexes for common lookups, and carefully model inventory and order state changes with transactions. For scale, I would consider caching, read replicas, and search services for product discovery.

### 42. How would you handle a table with hundreds of millions of rows?
I would review access patterns first, then add or refine indexes, archive cold data if appropriate, and consider partitioning. I would also check whether queries can be narrowed by date or tenant and whether expensive reporting should move to a separate analytical system.

### 43. How would you safely add a new non-null column to a large production table?
I would usually add it as nullable first, backfill in batches, update the application to write the new value, validate completeness, and only then enforce the non-null constraint. This reduces lock time and deployment risk.

### 44. How would you investigate high database CPU usage?
I would identify the top expensive queries, inspect execution plans, check waits and locks, review recent deployments, and confirm whether missing indexes, bad plans, parameter sniffing, or unexpected traffic spikes are involved.

### 45. How would you prevent duplicate records in a concurrent system?
I would use a unique constraint or unique index at the database level, because application-only checks are vulnerable to race conditions. Then I would handle duplicate-key errors gracefully in the application.

## Short SQL Prompts You May Be Asked

### 46. Find the second highest salary in a table.
One common answer is:

```sql
SELECT MAX(salary)
FROM employees
WHERE salary < (SELECT MAX(salary) FROM employees);
```

### 47. Find duplicate records based on email.
```sql
SELECT email, COUNT(*) AS duplicate_count
FROM users
GROUP BY email
HAVING COUNT(*) > 1;
```

### 48. Get the latest order per customer.
```sql
WITH ranked_orders AS (
    SELECT
        customer_id,
        order_id,
        order_date,
        ROW_NUMBER() OVER (
            PARTITION BY customer_id
            ORDER BY order_date DESC
        ) AS rn
    FROM orders
)
SELECT customer_id, order_id, order_date
FROM ranked_orders
WHERE rn = 1;
```

### 49. Explain the difference between COUNT(*), COUNT(1), and COUNT(column).
`COUNT(*)` counts all rows. `COUNT(1)` is generally treated the same by modern optimizers. `COUNT(column)` counts only rows where that column is not null.

### 50. What makes a strong database answer in interviews?
A strong answer balances correctness, tradeoffs, and practical experience. Good candidates explain not just what a concept is, but when it matters, what can go wrong, and how they have approached it in real systems.

## Quick Review Tips

- Focus on understanding tradeoffs, not memorizing buzzwords.
- Practice answering out loud in 60 to 90 seconds.
- Be ready with one or two real examples of query tuning, schema design, or production debugging.
- If the role is backend-heavy, expect follow-ups on transactions, indexing, and scalability.
- If the role is data-heavy, expect deeper questions on warehousing, partitioning, ETL, and analytics workloads.

## Flashcards

### Common Database Interview Questions
**Q:** What is a database?
**A:** A database is an organized system for storing, managing, and retrieving data efficiently. It allows applications and users to insert, update, query, and analyze information in a structured way.

**Q:** What is the difference between SQL and NoSQL databases?
**A:** SQL databases are relational, use structured schemas, and are typically queried with SQL. They are strong when you need joins, transactions, and strict consistency. NoSQL databases are often schema-flexible and are designed for use cases such as large-scale document storage, caching, graph traversal, or high-throughput distributed workloads.

**Q:** What is a primary key?
**A:** A primary key is a column or set of columns that uniquely identifies each row in a table. It must contain unique values and cannot be null.

**Q:** What is a foreign key?
**A:** A foreign key is a column or set of columns in one table that references the primary key of another table. It enforces referential integrity between related tables.

**Q:** What is normalization?
**A:** Normalization is the process of organizing data to reduce redundancy and improve data integrity. Common normal forms include 1NF, 2NF, and 3NF.

**Q:** When would you denormalize a database?
**A:** You denormalize when read performance is more important than strict normalization, especially in reporting, analytics, or high-read systems where reducing joins improves speed.

**Q:** What is the difference between DELETE, TRUNCATE, and DROP?
**A:** - `DELETE` removes selected rows and can be used with a `WHERE` clause.
- `TRUNCATE` removes all rows from a table quickly, usually with minimal logging.
- `DROP` removes the entire table structure and its data.

**Q:** What is the difference between WHERE and HAVING?
**A:** `WHERE` filters rows before grouping. `HAVING` filters groups after `GROUP BY` has been applied.

**Q:** What is a JOIN?
**A:** A JOIN combines rows from two or more tables based on a related column.

**Q:** What is the difference between INNER JOIN and LEFT JOIN?
**A:** `INNER JOIN` returns only matching rows from both tables. `LEFT JOIN` returns all rows from the left table and matching rows from the right table, with nulls when no match exists.

**Q:** What is an index?
**A:** An index is a data structure that improves lookup speed on one or more columns. It helps queries avoid full table scans, though it adds storage cost and can slow down inserts, updates, and deletes.

**Q:** What are constraints in a database?
**A:** Constraints are rules enforced on data to maintain integrity. Common examples are `PRIMARY KEY`, `FOREIGN KEY`, `UNIQUE`, `NOT NULL`, and `CHECK`.

**Q:** What is the difference between UNIQUE and PRIMARY KEY?
**A:** Both enforce uniqueness, but a primary key uniquely identifies the main record in a table and cannot be null. A table has one primary key, while it may have multiple unique constraints depending on the database engine.

**Q:** What is a transaction?
**A:** A transaction is a sequence of database operations treated as a single unit of work. It either fully succeeds and commits, or fails and rolls back.

**Q:** What does ACID mean?
**A:** - **Atomicity**: all operations in a transaction succeed or none do.
- **Consistency**: the database remains in a valid state.
- **Isolation**: concurrent transactions do not interfere improperly.
- **Durability**: committed changes survive failures.

**Q:** What is the difference between a clustered and non-clustered index?
**A:** A clustered index determines the physical order of data storage in the table. A non-clustered index stores a separate structure with pointers to the actual rows. A table generally has one clustered index but can have many non-clustered indexes.

**Q:** What is a composite index?
**A:** A composite index is an index on multiple columns. It is useful when queries commonly filter or sort by those columns together.

**Q:** What is the N+1 query problem?
**A:** The N+1 query problem happens when an application performs one query to fetch a list of records and then executes an additional query for each record to fetch related data. It causes unnecessary round trips and poor performance.

**Q:** What is a view?
**A:** A view is a virtual table based on the result of a query. It can simplify complex queries, improve reusability, and sometimes provide a security abstraction.

**Q:** What is a stored procedure?
**A:** A stored procedure is a reusable set of SQL statements stored in the database. It can encapsulate business logic, improve consistency, and reduce repeated SQL in applications.

### Advanced Database Interview Questions
**Q:** What are database isolation levels?
**A:** Isolation levels define how transactions interact with one another:
- `Read Uncommitted`: allows dirty reads.
- `Read Committed`: prevents dirty reads.
- `Repeatable Read`: prevents dirty and non-repeatable reads.
- `Serializable`: highest isolation, prevents dirty, non-repeatable, and phantom reads, but reduces concurrency.

**Q:** What are dirty reads, non-repeatable reads, and phantom reads?
**A:** - **Dirty read**: reading uncommitted data from another transaction.
- **Non-repeatable read**: getting different values when reading the same row twice in one transaction.
- **Phantom read**: seeing different sets of rows when rerunning a query in one transaction because another transaction inserted or deleted matching rows.

**Q:** What is a deadlock?
**A:** A deadlock happens when two or more transactions wait on each other to release locks, so none can proceed. Databases usually detect deadlocks and abort one transaction.

**Q:** How can you reduce the chance of deadlocks?
**A:** You can reduce deadlocks by accessing tables in a consistent order, keeping transactions short, indexing properly to reduce lock duration, and avoiding unnecessary user interaction inside transactions.

**Q:** What is optimistic locking vs pessimistic locking?
**A:** Optimistic locking assumes conflicts are rare and checks for changes before commit, often using a version column. Pessimistic locking locks data early to prevent concurrent modification, which is safer for contention-heavy scenarios but reduces concurrency.

**Q:** How would you optimize a slow SQL query?
**A:** Start by checking the execution plan. Look for full table scans, expensive joins, sorts, and missing indexes. Then reduce scanned rows, add or refine indexes, rewrite inefficient predicates, avoid unnecessary columns, and verify whether schema or query structure should change.

**Q:** What is an execution plan?
**A:** An execution plan shows how the database engine will execute a query, including scans, seeks, joins, sorts, and estimated or actual costs. It is one of the main tools for diagnosing performance issues.

**Q:** Why might a query not use an index?
**A:** Common reasons include low selectivity, outdated statistics, small table size, functions applied to indexed columns, implicit type conversions, leading wildcards in `LIKE`, or because the optimizer estimates a scan is cheaper.

**Q:** What is partitioning?
**A:** Partitioning splits a large table or index into smaller logical pieces while still presenting it as one object. It can improve manageability and, in some cases, query performance when filters align with the partition key.

**Q:** What is sharding?
**A:** Sharding distributes data across multiple independent database servers, usually by a shard key. It helps scale horizontally, but it increases complexity around joins, transactions, and rebalancing.

**Q:** What is replication?
**A:** Replication is the process of copying data from one database server to others. It is commonly used for high availability, disaster recovery, and read scaling.

**Q:** What is the CAP theorem?
**A:** The CAP theorem says that in a distributed system, during a network partition you can choose at most two of the following three guarantees: consistency, availability, and partition tolerance. Since partitions can happen, systems often make tradeoffs between consistency and availability.

**Q:** What is eventual consistency?
**A:** Eventual consistency means that if no new updates occur, all replicas will eventually converge to the same value. It is common in distributed systems that prioritize availability and performance.

**Q:** What is a covering index?
**A:** A covering index includes all the columns needed to satisfy a query, so the database can return results directly from the index without going back to the base table.

**Q:** What is cardinality in databases?
**A:** Cardinality can refer to relationship type, such as one-to-many, or to the number of distinct values in a column. In query optimization, higher cardinality often makes an index more useful.

**Q:** What is the difference between OLTP and OLAP?
**A:** OLTP systems handle many short, transactional operations such as inserts and updates in business applications. OLAP systems are optimized for analytical queries, aggregations, and reporting across large datasets.

**Q:** What are materialized views?
**A:** Materialized views store the results of a query physically, unlike standard views which are computed at query time. They improve read performance for expensive queries but require refresh management.

**Q:** What is database caching?
**A:** Database caching stores frequently accessed data in a faster layer, such as Redis or application memory, to reduce database load and improve latency.

**Q:** What are database statistics, and why do they matter?
**A:** Statistics describe data distribution and help the query optimizer estimate row counts and choose efficient execution plans. Poor or stale statistics can cause bad plans and slow queries.

**Q:** What is a schema migration?
**A:** A schema migration is a controlled change to the database structure, such as adding tables, columns, indexes, or constraints. Good migration practice includes versioning, rollback planning, and backward compatibility for live systems.

### Scenario-Based Questions and Answers
**Q:** How would you design a database for an e-commerce application?
**A:** I would start with core entities such as users, products, categories, carts, orders, order items, payments, and inventory. I would normalize transactional tables, add indexes for common lookups, and carefully model inventory and order state changes with transactions. For scale, I would consider caching, read replicas, and search services for product discovery.

**Q:** How would you handle a table with hundreds of millions of rows?
**A:** I would review access patterns first, then add or refine indexes, archive cold data if appropriate, and consider partitioning. I would also check whether queries can be narrowed by date or tenant and whether expensive reporting should move to a separate analytical system.

**Q:** How would you safely add a new non-null column to a large production table?
**A:** I would usually add it as nullable first, backfill in batches, update the application to write the new value, validate completeness, and only then enforce the non-null constraint. This reduces lock time and deployment risk.

**Q:** How would you investigate high database CPU usage?
**A:** I would identify the top expensive queries, inspect execution plans, check waits and locks, review recent deployments, and confirm whether missing indexes, bad plans, parameter sniffing, or unexpected traffic spikes are involved.

**Q:** How would you prevent duplicate records in a concurrent system?
**A:** I would use a unique constraint or unique index at the database level, because application-only checks are vulnerable to race conditions. Then I would handle duplicate-key errors gracefully in the application.

### Short SQL Prompts You May Be Asked
**Q:** Explain the difference between COUNT(*), COUNT(1), and COUNT(column).
**A:** `COUNT(*)` counts all rows. `COUNT(1)` is generally treated the same by modern optimizers. `COUNT(column)` counts only rows where that column is not null.

**Q:** What makes a strong database answer in interviews?
**A:** A strong answer balances correctness, tradeoffs, and practical experience. Good candidates explain not just what a concept is, but when it matters, what can go wrong, and how they have approached it in real systems.
